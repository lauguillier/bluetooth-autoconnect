[Unit]
Description=Bluetooth autoconnect service for pulseaudio
After=pulseaudio.service

[Service]
Type=simple
ExecStart=/usr/local/local/bin/bluetooth-autoconnect

[Install]
WantedBy=pulseaudio.service
